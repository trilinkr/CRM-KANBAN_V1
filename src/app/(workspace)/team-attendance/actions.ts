"use server";
import { and, eq } from "drizzle-orm";
import { revalidatePath } from "next/cache";
import { requireAdmin } from "@/lib/auth";
import { audit, db, attendanceSessions } from "@/lib/domain";
import { z } from "zod";
const correction = z.object({ sessionId: z.string().uuid(), checkInAt: z.string().datetime(), checkOutAt: z.string().datetime().nullable(), reason: z.string().trim().min(3).max(500) });
export async function correctAttendance(formData: FormData) { const admin = await requireAdmin(); const parsed = correction.safeParse({ sessionId: formData.get("sessionId"), checkInAt: new Date(String(formData.get("checkInAt"))).toISOString(), checkOutAt: formData.get("checkOutAt") ? new Date(String(formData.get("checkOutAt"))).toISOString() : null, reason: formData.get("reason") }); if (!parsed.success) throw new Error("Invalid attendance correction"); const original = (await db.select().from(attendanceSessions).where(eq(attendanceSessions.id, parsed.data.sessionId)).limit(1))[0]; if (!original) throw new Error("Attendance session not found"); const checkInAt = new Date(parsed.data.checkInAt); const checkOutAt = parsed.data.checkOutAt ? new Date(parsed.data.checkOutAt) : null; if (checkOutAt && checkOutAt <= checkInAt) throw new Error("Check-out must be after check-in"); const updated = (await db.update(attendanceSessions).set({ checkInAt, checkOutAt, correctedByUserId: admin.id, correctionReason: parsed.data.reason, updatedAt: new Date() }).where(and(eq(attendanceSessions.id, original.id), eq(attendanceSessions.userId, original.userId))).returning())[0]; await audit(admin.id, "ATTENDANCE_CORRECTED", "ATTENDANCE_SESSION", updated.id, { employeeId: original.userId, original: { checkInAt: original.checkInAt.toISOString(), checkOutAt: original.checkOutAt?.toISOString() ?? null }, corrected: { checkInAt: updated.checkInAt.toISOString(), checkOutAt: updated.checkOutAt?.toISOString() ?? null }, reason: parsed.data.reason }); revalidatePath("/team-attendance"); revalidatePath(`/team-attendance/${original.userId}`); }
