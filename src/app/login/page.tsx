import { login } from "./actions";
import { Button, Input } from "@/components/ui";

export default function LoginPage() { return <main className="flex min-h-screen items-center justify-center px-6"><div className="w-full max-w-sm"><div className="mb-8"><div className="mb-4 flex h-10 w-10 items-center justify-center rounded-xl bg-brand text-lg font-bold text-white">T</div><h1 className="text-3xl font-semibold tracking-tight">Welcome back</h1><p className="mt-2 text-sm text-muted">Sign in to your TriLinkr workspace.</p></div><form action={login} className="space-y-4 rounded-2xl border border-line bg-white p-6 shadow-soft"><label className="block text-sm font-medium">Work email<Input name="email" type="email" required placeholder="you@trilinkr.com" className="mt-2" /></label><label className="block text-sm font-medium">Password<Input name="password" type="password" required className="mt-2" /></label><Button className="w-full">Sign in</Button></form><p className="mt-5 text-center text-xs text-muted">TriLinkr Private Limited · Internal use only</p></div></main>; }
